query GetProductByHandle($handle: String!, $cursor: String, $limit: Int) {
  getProductByHandle(handle: $handle, limit: $limit, cursor: $cursor) {
    id
    title
    handle
    description
    priceRange {
      min
      max
    }
    options {
      name
      values
    }
    featuredMedia {
      src
      thumbnailSrc
      id
      src
      type
    }
    media {
      edges {
        node {
          thumbnailSrc
          id
          src
          type
        }
      }
    }
    variants {
      edges {
        node {
          id
          title
          price
        }
      }
    }
  }
}
